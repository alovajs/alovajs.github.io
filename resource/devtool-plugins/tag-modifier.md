The `tagModifier` plugin — which rewrites API `tags` — is now part of worma's built-in plugins. See the worma [tag-modifier plugin](https://worma.js.org/docs/plugin-system/builtin-plugins/tag-modifier) docs.
