The `rename` plugin — which renames API URLs and parameters with configurable naming styles — is now part of worma's built-in plugins. See the worma [rename plugin](https://worma.js.org/docs/plugin-system/builtin-plugins/rename) docs.
